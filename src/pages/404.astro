---
// src/pages/404.astro
import BaseLayout from "../layouts/BaseLayout.astro";
import Button from "../components/ui/Button.astro";

// Nota: Como 404.astro se sirve en la raíz, a veces es difícil saber el idioma.
// Aquí ponemos textos bilingües simples o por defecto en español.
---

<BaseLayout lang="es" alternateUrl="">
    <Fragment slot="head">
    <title>404 - Página no encontrada</title>
    <meta name="description" content="Página no encontrada" />
  </Fragment>
  <main class="min-h-screen flex items-center justify-center bg-neutral-950 relative overflow-hidden">
    
    {/* Background Effects */}
    <div class="absolute inset-0 pointer-events-none">
      {/* Grid Pattern */}
      <div class="absolute inset-0 opacity-20" 
           style="background-image: radial-gradient(circle at 1px 1px, rgb(100 116 139 / 0.15) 1px, transparent 0); background-size: 40px 40px;">
      </div>
      
      {/* Gradient Blobs */}
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-primary-500/20 rounded-full blur-[100px] animate-pulse"></div>
      <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-secondary-500/20 rounded-full blur-[100px] animate-pulse" style="animation-delay: 1s;"></div>
    </div>

    <div class="relative z-10 text-center px-6 max-w-2xl mx-auto">
      
      {/* 404 GLITCH EFFECT */}
      <div class="relative mb-8">
        <h1 class="text-9xl md:text-[12rem] font-bold text-transparent bg-clip-text bg-gradient-to-b from-neutral-200 to-neutral-800 tracking-tighter select-none">
          404
        </h1>
        {/* Ghost Icon Floating */}
        <div class="absolute -top-12 -right-4 md:-right-12 animate-bounce duration-[3000ms]">
          <svg class="w-24 h-24 text-primary-400 drop-shadow-[0_0_15px_rgba(45,212,191,0.5)]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 10a2 2 0 100-4 2 2 0 000 4zM15 10a2 2 0 100-4 2 2 0 000 4z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 16c1.5 1 4.5 1 6 0" />
          </svg>
        </div>
      </div>

      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
        ¡Yohohoho! Te has perdido.
      </h2>
      
      <p class="text-lg text-neutral-400 mb-10 text-pretty">
        Esta página es como mi piel... <strong>¡No existe!</strong> (Porque soy un esqueleto).
        <br />
        Parece que el enlace está roto o la dirección es incorrecta.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button href="/" variant="default" class="shadow-xl shadow-primary-500/20">
          <svg class="w-5 h-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
          </svg>
          Volver al Inicio
        </Button>
        
        <Button href="/es/contacto" variant="ghost">
          Reportar el error
        </Button>
      </div>

    </div>
  </main>
</BaseLayout>