---
import LandingServicios from "../../../components/pages/LandingServicios.astro";
import t_page from "../../../i18n/es/services.json" with { type: "json" };

const alternateUrl = new URL(t_page.alternateUrl, Astro.site).href;

const schema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: `${t_page.copy.hero.h1_part1} ${t_page.copy.hero.h1_part2}`,
  description: t_page.metadata.description,
  provider: {
    "@type": "LocalBusiness",
    name: "SanahujaDev",
    areaServed: "Tenerife, Canarias, España",
  },
};
---
<LandingServicios 
  copy={t_page}
  alternateUrl={alternateUrl}
  schema={schema}
/>
